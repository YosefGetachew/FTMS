PORT=5000
DB_USER=postgres
DB_HOST=localhost
DB_NAME=ftms
DB_PASSWORD=password
DB_PORT=5432
JWT_SECRET=secretkey

# Optional SMS gateway for travel status and approval task alerts.
# Leave SMS_ENABLED=false until your provider URL/token are ready.
SMS_ENABLED=false
SMS_API_URL=
SMS_API_TOKEN=
SMS_API_METHOD=POST
SMS_FROM=FTMS
SMS_TO_FIELD=to
SMS_MESSAGE_FIELD=message
SMS_FROM_FIELD=from
